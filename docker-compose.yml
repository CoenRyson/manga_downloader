services:
  manga-downloader:
    build: .
    image: manga-downloader:latest
    container_name: manga-downloader
    restart: unless-stopped
    environment:
      PORT: "3030"
      HOST: "0.0.0.0"
    ports:
      - "3030:3030"